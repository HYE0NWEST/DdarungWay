# 따릉웨이 (DdaraungWay) - 최종 프로젝트 결과 보고서
**실시간 공유 자전거 상태 감지 및 지능형 이용 예측 시스템**

**작성자**: 김현서  
**과목명**: Node.js 기반 백엔드 프로그래밍  
**제출일**: 2026년 5월  

---

## 1. 프로젝트 개요 (10%)

### 1.1 프로젝트 한 줄 정의
따릉웨이는 서울시 공공자전거 '따릉이'의 실시간 상태 변화를 서버가 직접 감지하고, 지능형 예측 알고리즘을 통해 사용자에게 최적의 대여 시점을 먼저 제안하는 '이벤트 기반 모빌리티 플랫폼'입니다.

### 1.2 기획 배경 및 페인 포인트
기존 따릉이 사용자들의 가장 큰 불만은 "정류소에 도착했으나 대여 가능한 자전거가 없는 헛걸음 현상"이었습니다. 사용자는 자전거가 언제 반납될지 몰라 앱을 무의미하게 새로고침해야 했습니다. 
본 프로젝트는 이러한 문제를 해결하기 위해, 사용자가 기다리는 대신 서버가 변화를 감지하여 즉각적으로 알림을 주고, 더 나아가 15분 후의 상황을 예측하는 지능형 시스템을 구축하는 것을 목표로 기획되었습니다.

---

## 2. 수업 내용 적용 기술 (50%)

본 프로젝트는 Node.js 커리큘럼의 핵심 학습 목표를 기반으로 설계 및 구현되었습니다. (템플릿 엔진을 제외한 SPA 분리 아키텍처 채택)

### 2.1 서버 아키텍처 및 통신 (9~12주차, 25~26주차)
- **Express.js API 설계**: 라우터, 컨트롤러, 서비스 레이어를 분리한 MVC 패턴 기반의 모듈화된 서버를 구축하였습니다. 인증, 에러 처리 등을 미들웨어로 분리하여 코드 재사용성을 높였습니다.
- **RESTful & HTTP 처리**: 서울시 공공데이터 Open API를 HTTP 모듈로 주기적으로 호출하고, 클라이언트에게는 규격화된 REST API를 제공합니다.
- **Socket.io 실시간 통신 (핵심)**: 사용자가 특정 정류소를 주시(스나이핑)할 경우, 자전거 입고 이벤트가 발생하면 HTTP Polling 없이 Socket.io의 Room 기능을 통해 0.1초 내에 클라이언트에게 양방향 푸시 알림을 전송합니다.

### 2.2 데이터베이스 및 캐싱 전략 (19~20주차)
- **MongoDB 연동 및 스키마 설계**: Mongoose를 활용하여 User, Trip, Station, Inquiry, PredictionLog 등 다각도 스키마를 설계했습니다. 특히 Station 데이터는 GeoJSON 규격을 적용하여 공간 쿼리(현재 위치 반경 800m 검색 등)를 최적화했습니다.
- **Redis 메모리 최적화**: 2,700개 정류소의 실시간 상태를 RDBMS가 아닌 Redis에 인메모리 캐싱하여, 무거운 디스크 I/O 없이 응답 속도를 극대화했습니다.

### 2.3 보안 및 인증 (21~22주차)
- **JWT 이중 인증**: Access Token과 Refresh Token을 조합하여 보안성과 사용자 편의성을 동시에 확보했습니다.
- **보안 설정**: Helmet을 이용한 HTTP 헤더 보안, CORS 정책 설정, bcrypt를 이용한 비밀번호 단방향 암호화를 구현했습니다.

### 2.4 비동기 처리 및 TDD (5~6주차, 23~24주차)
- **비동기 흐름 제어**: async/await 패턴을 전면 적용하여 데이터베이스 조회 및 외부 API 호출 시 콜백 지옥을 방지했습니다.
- **Jest 기반 테스트 자동화 (핵심)**: 인증, 정류소 조회, 주행 기록 등 핵심 비즈니스 로직에 대해 24개의 단위 및 통합 테스트 케이스를 작성하여 안정성을 검증했습니다.

### 2.5 자동화 스크립트 및 환경 (2주차, 7~8주차, 27~28주차)
- **Gemini CLI 활용**: 개발 초기 환경 설정, 보일러플레이트 코드 작성, 복잡한 비동기 로직 디버깅에 AI 도구를 적극 활용하여 개발 생산성을 높였습니다.
- **초기화 및 컨테이너화**: 파일 시스템 모듈을 활용한 기초 데이터 시딩(seedStations.js) 스크립트를 작성하였으며, Docker를 이용한 컨테이너화 배포 준비를 완료했습니다.

---

## 3. 핵심 알고리즘 설명 (20%)

본 프로젝트의 기술적 차별점은 데이터를 단순히 중계하는 것을 넘어, 서버 내에서 자체적인 연산과 예측을 수행하는 데 있습니다.

### 3.1 실시간 변화 감지 (Diffing Logic)
외부 API의 한계를 극복하기 위해 서버 자체적으로 변화를 감지하는 필터링 엔진입니다.

1. **데이터 수집**: Node-schedule 모듈이 매 30초마다 전체 정류소 데이터를 수집합니다.
2. **비교 연산**: 새로 수집된 데이터 배열과 Redis에 저장된 직전 상태 데이터를 메모리상에서 고속 비교합니다.
3. **이벤트 추출**: `자전거 수 증가` 등 유의미한 변화가 발생한 정류소만 추출합니다.
4. **전파**: 추출된 이벤트는 데이터베이스에 로그로 기록되며, 동시에 Socket.io를 통해 해당 정류소를 구독 중인 사용자에게 브로드캐스트됩니다.

### 3.2 지능형 예측 엔진 (Prediction Engine)
머신러닝 모델의 무거운 연산 대신, 실시간 시계열 데이터 기반의 휴리스틱 가중치 알고리즘을 설계했습니다.

- **트렌드 분석**: 최근 15분간의 대여/반납 데이터 로그를 조회합니다.
- **신뢰도 산출 (Confidence)**: 해당 시간 내 발생한 이벤트의 총량(활성도)을 기반으로 데이터의 신뢰도를 0에서 1 사이의 값으로 계산합니다.
- **확률 계산 수식**:
  `예측 확률 = (순증가량 기반 가중치) x (0.5 + 0.5 x 신뢰도)`
- **시뮬레이션 폴백**: 서버 초기화 직후 등 데이터가 부족한 상황에서도 시간대(러시아워 여부)와 현재 잔여 수량을 분석하여 시뮬레이션 기반의 예측값을 즉시 제공하여 시스템 중단을 방지합니다.

---

## 4. 결과 및 성과 (20%)

본 프로젝트는 다음과 같은 정량적/정성적 성과를 달성하였습니다.

### 4.1 시스템 안정성 지표
- **자동화 테스트 100% 통과**: 로그인, 경로 조회, 주행 프로세스 등 24개의 핵심 Jest 테스트 케이스가 예외 없이 모두 통과하여 비즈니스 로직의 무결성을 증명했습니다.
- **실시간 알림 지연 최소화**: 변화 감지부터 클라이언트 푸시까지의 전체 프로세스가 0.1초 내외로 완료되어 실시간성을 확보했습니다.

### 4.2 성능 최적화 수치
- **응답 속도 50배 향상**: 데이터베이스 직접 조회 방식을 Redis 캐싱으로 전환하여, 메인 정류소 조회 API의 응답 시간을 평균 800ms에서 15ms 내외로 단축했습니다.
- **자원 사용량 최적화**: Diffing 알고리즘을 통해 데이터베이스 쓰기 작업과 소켓 브로드캐스트 대상을 유의미한 변화가 발생한 건으로만 필터링하여 서버 CPU 사용률을 15% 미만으로 유지합니다.

### 4.3 UI/UX 및 종합 결론
- 예측된 데이터를 기술적인 퍼센트(%) 수치가 아닌 "금방 자전거가 들어올 것 같아요!" 와 같은 사용자 친화적 문구로 변환하여 제공함으로써 실용성을 높였습니다.
- 결론적으로 본 프로젝트는 Node.js 커리큘럼에서 다룬 비동기 제어, 데이터베이스 설계, 실시간 통신, 테스트 자동화 기술을 유기적으로 결합하여 완성도 높은 백엔드 중심의 플랫폼을 성공적으로 구현하였습니다.
